=breadcrumb_tag do
  %li.active
    Роли
  %li
    Администрирование

.page-header
  %h1= t('headers.roles.index')

= link_to new_role_path, class: 'btn btn-success' do
  = fa_icon :plus
  = t('buttons.new.role')

.index_table
  .row
    .col-md-4.center= Role.human_attribute_name(:name)
    .col-md-4.center= Role.human_attribute_name(:info)
    .col-md-4.center= Role.human_attribute_name(:full_info)

  - @roles.each do |role|
    .row
      .col-md-4.center= role.name
      .col-md-4.center= role.info
      .col-md-4.center.bottom20= role.full_info
      .col-md-12.center
        = link_to role, class: 'btn btn-warning' do
          = fa_icon :eye
          = t('buttons.show')
        = link_to edit_role_path(role), class: 'btn btn-info' do
          = fa_icon :edit
          = t('buttons.edit')
        = link_to role, :method => :delete,
        :data => { :confirm => t('buttons.confirm.role') },
        class: 'btn btn-danger' do
          = fa_icon :times
          = t('buttons.destroy')
